# HEARTBEAT.md

- Check system thermal status and CPU load.
- If CPU usage is > 80% or thermal warnings appear, notify Isak.
- Ensure Amphetamine or pmset settings are still keeping the Mac awake.
- Check the `drop/` folder for new files. If found:
    - Append them to `INBOX.md` as "- [ ] Processed file from drop: <filename>".
    - Move processed files to `drop/processed/`.
- Check `INBOX.md` for any urgent tasks.
